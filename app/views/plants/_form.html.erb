<%= form_with(model: plant, local: true) do |form| %>
  <% if @plant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(plant.errors.count, "error") %> prohibited this plant from being saved:</h2>

      <ul>
      <% plant.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label "nazwa: " %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <th>Gatunek: </th>
    <%= form.collection_select(:species_id, Species.all, :id, :name) %>

  </div>

  <div class="field">
    <%= form.label "Status zdrowia: " %>
    <%= form.select(:health_status, Plant.health_statuses.keys) %>    
  </div>

  <div class="field">
    <%= form.label "Co ile dni podlewać: " %>
    <%= form.number_field :irrigation_frequency_in_days %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
